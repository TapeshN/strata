---
title: "Invalid HTML hydrates nowhere: a card that renders its own `<li>` inside a list's `<li>`"
date: 2026-09-11
category: infra
tags: ["hydration", "surface-contract", "nested-li"]
confidence: learned
source: private-work
---

a component rendered only inside a list row roots at `<div>`; when a section digest moves with no token change, rebind the last amendment and say why — never a fake add.
