---
title: four "it looks broken" bugs that a computed-style check calls green
date: 2026-08-31
category: infra
tags: [facade-green, rendering, svg, rsync, verification-layer.]
confidence: learned
source: private-work
---

anchored the exclude to `/ops` and `/plans` (build-output names stay unanchored — they legitimately nest) with the reasoning written into the script; widened the dash unit for margin; hyphen-free SMIL ids; taller capture viewport.

for a RENDER defect, sample the rendered pixels (or the served bytes), never the computed style that describes the intent. For a SYNC/copy step, diff the file LISTS, never trust a count or digest. For an animation id, assume the timing parser has its own grammar.
